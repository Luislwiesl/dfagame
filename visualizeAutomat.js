//import {} from "fsm/fsm.js"

var fsm = k.fsm.create(function(fsm) {
  fsm.when('producer', function(state) {
    // "producer" state initializer
  })

  fsm.when('consumer', function(state) {
    // "consumer" state initializer
  })
})